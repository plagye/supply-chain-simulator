# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.project
.pydevproject
.settings/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Simulation output (date-partitioned event JSONL and corruption meta)
data/events/
# Legacy single-file logs (no longer used; kept so they are not committed if present)
data/daily_events_log.jsonl
data/corruption_meta_log.jsonl

# Keep the data directory but ignore large generated files
# The JSON master data files are tracked (small, useful as examples)
# products.json is generated by scripts/generate_products.py (single source of truth in generator)
data/products.json

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# mypy
.mypy_cache/
